#!/usr/bin/env plackup

use strict;
use warnings;

use Plack::App::CGIBin;
use Plack::Builder;

my $app = Plack::App::CGIBin->new(root => './www/cgi-bin')->to_app;
builder {
    mount '/cgi-bin' => $app;
};

# vim: ft=perl
